// Généré par node scripts/build-courses.cjs — modifier les fichiers dans cours/.
const courseLessons = [
  {
    "id": "awk",
    "label": "awk",
    "group": "Texte et flux",
    "summary": "Sélectionne et traite des colonnes ligne par ligne.",
    "html": "<h2>Comprendre</h2>\n<p>Sélectionne et traite des colonnes ligne par ligne.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>awk &#39;condition {action}&#39; fichier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>awk &#39;$4==&quot;ERREUR&quot; {print $3}&#39; incidents.log</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>$1</code> est la première colonne. L’option <code>-F,</code> définit la virgule comme séparateur.</p>"
  },
  {
    "id": "cat",
    "label": "cat",
    "group": "Texte et flux",
    "summary": "Affiche et concatène le contenu de fichiers sur la sortie standard.",
    "html": "<h2>Comprendre</h2>\n<p><code>cat</code>, abréviation de <strong>concatenate</strong>, lit un ou plusieurs fichiers puis écrit leur contenu sur la sortie standard. Dans un terminal, cette sortie apparaît directement à l’écran.</p>\n<p>La commande ne modifie pas les fichiers qu’elle lit. Elle est adaptée aux fichiers texte courts. Pour un document long, <code>less</code> offre une navigation plus confortable.</p>\n<h2>Commandes et options</h2>\n<h3>Afficher un fichier</h3>\n<pre><code>cat accueil/briefing</code></pre>\n<p><code>cat fichier</code> affiche tout le contenu du fichier, de la première à la dernière ligne. Si le chemin est incorrect, le shell signale que le fichier n’existe pas.</p>\n<h3>Afficher plusieurs fichiers</h3>\n<pre><code>cat partie1 partie2</code></pre>\n<p>Les fichiers sont lus dans l’ordre indiqué. Le contenu de <code>partie2</code> est affiché juste après celui de <code>partie1</code>. Les fichiers sources restent séparés et inchangés.</p>\n<h3>Numéroter les lignes</h3>\n<pre><code>cat -n accueil/briefing</code></pre>\n<p>L’option <code>-n</code> ajoute un numéro devant chaque ligne affichée. Elle permet de repérer rapidement une position dans un petit fichier.</p>\n<h3>Envoyer le contenu dans un pipe</h3>\n<pre><code>cat flux/chronologie | head -n 1</code></pre>\n<p>Le pipe <code>|</code> transmet la sortie de <code>cat</code> à la commande suivante. Ici, <code>head</code> conserve uniquement la première ligne du contenu reçu.</p>\n<h3>Concaténer dans un nouveau fichier</h3>\n<pre><code>cat partie1 partie2 &gt; complet\ncat annexe &gt;&gt; complet</code></pre>\n<p><code>&gt;</code> enregistre la concaténation dans <code>complet</code> et remplace son ancien contenu. <code>&gt;&gt;</code> ajoute le contenu d’<code>annexe</code> à la fin sans effacer ce qui existe déjà.</p>\n<h2>Exemple commenté</h2>\n<p>Imagine que <code>accueil/briefing</code> contient trois lignes.</p>\n<pre><code>$ cat accueil/briefing\nBienvenue dans le Module 02\nLis chaque question avec attention\nFLAG{EXEMPLE}\n$ cat -n accueil/briefing\n     1  Bienvenue dans le Module 02\n     2  Lis chaque question avec attention\n     3  FLAG{EXEMPLE}</code></pre>\n<p>La première commande affiche le texte tel qu’il est enregistré. La seconde conserve le même contenu et ajoute seulement des numéros dans la sortie du terminal.</p>\n<h2>Points de vigilance</h2>\n<p><code>cat</code> affiche tout le fichier sans pause. Utilise <code>less</code> si le contenu risque de remplir plusieurs écrans.</p>\n<p>La commande <code>cat fichier</code> ne modifie pas le fichier. Le danger vient surtout des redirections. <code>cat source &gt; source</code> vide le fichier avant que <code>cat</code> puisse le lire.</p>\n<p>Un fichier binaire peut produire des caractères illisibles dans le terminal. Utilise d’abord <code>file</code> lorsque la nature du fichier est inconnue.</p>"
  },
  {
    "id": "cd",
    "label": "cd",
    "group": "Navigation",
    "summary": "Change le dossier courant et navigue avec des chemins relatifs ou absolus.",
    "html": "<h2>Comprendre</h2>\n<p><code>cd</code>, abréviation de <strong>change directory</strong>, permet de changer le dossier courant du terminal. Ce dossier sert de point de départ à tous les chemins relatifs utilisés ensuite par <code>ls</code>, <code>cat</code>, <code>cp</code> ou d’autres commandes.</p>\n<p>La commande <code>pwd</code> affiche le chemin absolu du dossier courant. Ce chemin commence à la racine <code>/</code>. Lorsqu’un changement réussit, <code>cd</code> n’affiche généralement rien. Utilise <code>pwd</code> pour confirmer ta nouvelle position.</p>\n<h2>Commandes et options</h2>\n<p>La forme générale est <code>cd chemin</code>. Le chemin peut désigner un dossier situé sous le dossier courant, un dossier parent, le dossier personnel ou un emplacement absolu.</p>\n<h3>Revenir dans son dossier personnel</h3>\n<pre><code>cd &quot;$HOME&quot;\ncd\ncd ~</code></pre>\n<p>La variable d’environnement <code>$HOME</code> contient le chemin absolu de ton dossier personnel. Sa valeur ressemble généralement à <code>/home/alice</code>.</p>\n<ul><li><code>cd &quot;$HOME&quot;</code> utilise directement la valeur de la variable <code>HOME</code>.</li><li><code>cd</code> sans argument utilise lui aussi le dossier indiqué par <code>HOME</code>.</li><li><code>cd ~</code> fonctionne de la même façon dans Bash. Le shell remplace <code>~</code> par le chemin du dossier personnel avant d’exécuter <code>cd</code>.</li></ul>\n<p><code>~</code> est rapide à saisir. <code>$HOME</code> est particulièrement utile dans les scripts ou pour construire un chemin comme <code>cd &quot;$HOME/rep1&quot;</code>. Les guillemets protègent la valeur de la variable si le chemin contient des espaces.</p>\n<h3>Descendre dans un ou plusieurs dossiers</h3>\n<pre><code>cd rep1\ncd rep1/rep2</code></pre>\n<p><code>cd rep1</code> entre dans le dossier <code>rep1</code> situé dans le dossier courant. <code>cd rep1/rep2</code> traverse deux niveaux en une commande. <code>rep1</code> doit exister dans le dossier courant et <code>rep2</code> doit se trouver dans <code>rep1</code>.</p>\n<p>Ces deux chemins sont <strong>relatifs</strong>. Leur destination dépend du dossier de départ. Si tu changes de position, le même chemin peut mener ailleurs ou ne plus exister.</p>\n<h3>Remonter dans l’arborescence</h3>\n<pre><code>cd ..\ncd ../..\ncd ../../rep1</code></pre>\n<p><code>..</code> représente le dossier parent. <code>cd ..</code> remonte donc d’un niveau et <code>cd ../..</code> remonte de deux niveaux. Les éléments peuvent être combinés. <code>cd ../../rep1</code> remonte deux fois, puis entre dans <code>rep1</code> depuis ce nouvel emplacement.</p>\n<h3>Revenir au dossier précédent</h3>\n<pre><code>cd &quot;$OLDPWD&quot;\ncd -</code></pre>\n<p>La variable d’environnement <code>$OLDPWD</code> mémorise le dossier occupé avant le dernier changement. <code>cd &quot;$OLDPWD&quot;</code> utilise cette valeur pour rejoindre ce dossier. <code>cd -</code> est une forme plus courte qui fait le même déplacement et affiche le chemin rejoint.</p>\n<h3>Utiliser un chemin absolu</h3>\n<pre><code>cd /etc\ncd /\ncd &quot;$HOME/rep1/rep2&quot;</code></pre>\n<p>Un chemin commençant par <code>/</code> est <strong>absolu</strong>. Il part toujours de la racine du système, quelle que soit ta position actuelle. <code>cd /etc</code> rejoint le dossier système <code>/etc</code>. <code>cd /</code> rejoint directement la racine. Après développement de <code>$HOME</code>, le troisième exemple devient lui aussi un chemin absolu.</p>\n<h3>Rester sur place ou gérer les espaces</h3>\n<pre><code>cd .\ncd &quot;Mes documents&quot;\ncd Mes\\ documents</code></pre>\n<p><code>.</code> représente le dossier courant. <code>cd .</code> ne change donc pas de position. Pour un nom contenant des espaces, entoure le chemin de guillemets ou protège chaque espace avec <code>\\</code>. Sans cela, le shell interprète les mots comme plusieurs arguments.</p>\n<h2>Exemple commenté</h2>\n<p>Imagine que le dossier personnel est <code>/home/alice</code> et que <code>rep1</code> contient lui-même <code>rep2</code>.</p>\n<pre><code>$ pwd\n/home/alice\n$ cd rep1\n$ pwd\n/home/alice/rep1\n$ cd rep2\n$ pwd\n/home/alice/rep1/rep2\n$ cd ../..\n$ pwd\n/home/alice\n$ cd rep1/rep2\n$ pwd\n/home/alice/rep1/rep2\n$ cd ~\n$ pwd\n/home/alice</code></pre>\n<p>Le premier <code>cd rep1</code> mène dans <code>/home/alice/rep1</code>. Comme la position a changé, <code>cd rep2</code> peut ensuite atteindre <code>/home/alice/rep1/rep2</code>. Depuis cet emplacement, <code>cd ../..</code> remonte deux niveaux et revient dans <code>/home/alice</code>. <code>cd rep1/rep2</code> montre qu’il est également possible de refaire tout le trajet avec un seul chemin relatif. Enfin, <code>cd ~</code> revient au dossier personnel, indépendamment du point de départ.</p>\n<h2>Points de vigilance</h2>\n<ul><li>Linux distingue les majuscules et les minuscules. <code>Documents</code> et <code>documents</code> sont deux dossiers différents.</li><li><code>cd</code> suivi d’un nom incorrect produit le message <code>No such file or directory</code>.</li><li>Il faut avoir le droit de traverser le dossier. Sans le droit d’exécution <code>x</code>, le shell répond <code>Permission denied</code>.</li><li><code>~</code> est développé seulement lorsqu’il apparaît au début d’un mot et qu’il n’est pas protégé par des guillemets. Utilise <code>cd ~</code>, pas <code>cd &quot;~&quot;</code>. <code>cd &quot;$HOME&quot;</code> fonctionne car les variables sont développées entre guillemets doubles.</li><li>Un chemin commençant par <code>/</code> est absolu. Les autres chemins vus ici sont relatifs au dossier courant, sauf <code>~</code>, <code>$HOME</code> et <code>$OLDPWD</code> que le shell développe avant d’exécuter <code>cd</code>.</li><li>Dans un script destiné à plusieurs utilisateurs, évite un chemin écrit en dur comme <code>/home/alice</code>. Utilise <code>~</code> ou <code>$HOME</code> afin d’employer le dossier personnel du compte qui exécute le script.</li></ul>"
  },
  {
    "id": "chmod",
    "label": "chmod",
    "group": "Fichiers et droits",
    "summary": "Règle les droits de lecture, d’écriture et d’exécution.",
    "html": "<h2>Comprendre</h2>\n<p><code>chmod</code> modifie les droits d’accès d’un fichier ou d’un dossier. Ces droits sont organisés en <strong>trois triplets</strong> pour le propriétaire (<code>u</code>), le groupe (<code>g</code>) et les autres utilisateurs (<code>o</code>). Dans chaque triplet, <code>r</code> signifie lecture, <code>w</code> écriture et <code>x</code> exécution.</p>\n<p>Le premier caractère de <code>ls -l</code> décrit le type (<code>-</code> pour fichier, <code>d</code> pour dossier). Les neuf caractères suivants représentent les trois triplets. Dans <code>-rwxr-x---</code>, le propriétaire possède <code>rwx</code>, le groupe possède <code>r-x</code> et les autres n’ont aucun droit.</p>\n<h2>Commandes et options</h2>\n<ul><li>La forme symbolique ajoute ou retire un droit précis. <code>chmod u+x fichier</code> ajoute <code>x</code> au propriétaire. <code>chmod g-w fichier</code> retire <code>w</code> au groupe. <code>chmod o= fichier</code> enlève tous les droits aux autres.</li><li>Avec la forme numérique, chaque triplet devient un chiffre obtenu en additionnant <code>r = 4</code> (<code>2²</code>), <code>w = 2</code> (<code>2¹</code>) et <code>x = 1</code> (<code>2⁰</code>). Une absence de droit vaut <code>0</code>.</li><li>Le chiffre <code>7</code> donne <code>rwx</code>, <code>6</code> donne <code>rw-</code> et <code>5</code> donne <code>r-x</code>.</li><li><code>chmod 600 fichier</code> donne <code>rw-------</code>. <code>chmod 755 script.sh</code> donne <code>rwxr-xr-x</code>. <code>chmod 700 dossier</code> donne <code>rwx------</code>.</li></ul>\n<h2>Exemple commenté</h2>\n<pre><code>mkdir travail/prive\ncp donnees/config/app.conf travail/prive/app.conf\nchmod 700 travail/prive\nchmod 600 travail/prive/app.conf\nls -ld travail/prive\nls -l travail/prive/app.conf</code></pre>\n<p>Le dossier reçoit <code>7</code> pour son propriétaire. Il peut le lister, y créer des entrées et le traverser. Le fichier reçoit <code>6</code>. Son propriétaire peut le lire et le modifier, mais pas l’exécuter. Dans les deux cas, le groupe et les autres n’ont aucun droit.</p>\n<h2>Points de vigilance</h2>\n<p>Sur un <strong>dossier</strong>, <code>x</code> permet surtout de le traverser et d’accéder à une entrée dont on connaît le nom. <code>r</code> permet de lister ses noms. <code>w</code> permet d’y créer ou supprimer des entrées, généralement avec <code>x</code>. Sur un <strong>fichier</strong>, <code>x</code> autorise son exécution. <code>chmod</code> ne change pas le propriétaire. Ce rôle revient à <code>chown</code>. Évite <code>chmod -R 777</code> car il ouvre largement tout un arbre, y compris les fichiers qui n’ont pas besoin d’être exécutables.</p>"
  },
  {
    "id": "chown",
    "label": "chown",
    "group": "Fichiers et droits",
    "summary": "Change le propriétaire et, éventuellement, le groupe.",
    "html": "<h2>Comprendre</h2>\n<p>Change le propriétaire et, éventuellement, le groupe.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>chown utilisateur:groupe fichier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>chown alice:admins rapport.txt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Changer de propriétaire demande généralement des droits administrateur. L’exemple est une syntaxe, pas une commande à lancer dans l’atelier.</p>"
  },
  {
    "id": "cp",
    "label": "cp",
    "group": "Fichiers et droits",
    "summary": "Copie un fichier sans retirer la source.",
    "html": "<h2>Comprendre</h2>\n<p>Copie un fichier sans retirer la source.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>cp source destination</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>cp donnees/notes/depart.txt travail/depart.txt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Si la destination existe, elle peut être remplacée.</p>"
  },
  {
    "id": "cut",
    "label": "cut",
    "group": "Texte et flux",
    "summary": "Extrait des champs ou des positions de caractères dans chaque ligne.",
    "html": "<h2>Comprendre</h2>\n<p><code>cut</code> sélectionne une partie de chaque ligne reçue. La commande peut découper les lignes en champs séparés par un caractère ou conserver des positions précises.</p>\n<p>Le résultat est écrit sur la sortie standard. Le fichier source reste inchangé et les lignes conservent leur ordre d’origine.</p>\n<h2>Commandes et options</h2>\n<h3>Choisir le séparateur</h3>\n<pre><code>cut -d: -f2 colonnes/fiche</code></pre>\n<p>L’option <code>-d</code> définit le délimiteur qui sépare les champs. Ici, <code>-d:</code> utilise le caractère <code>:</code>. L’option <code>-f2</code> conserve le deuxième champ de chaque ligne.</p>\n<h3>Extraire un autre champ</h3>\n<pre><code>cut -d: -f3 colonnes/fiche\ncut -d&#39;;&#39; -f2 colonnes/equipe</code></pre>\n<p>Le séparateur doit correspondre au fichier. Les guillemets protègent le point-virgule afin que le shell ne l’interprète pas comme une séparation entre commandes.</p>\n<h3>Extraire plusieurs champs</h3>\n<pre><code>cut -d: -f1,3 colonnes/fiche\ncut -d: -f2-4 colonnes/fiche</code></pre>\n<p>Une virgule sélectionne plusieurs champs distincts. Un tiret sélectionne une plage continue.</p>\n<h3>Extraire des caractères</h3>\n<pre><code>cut -c1-8 colonnes/fiche</code></pre>\n<p>L’option <code>-c</code> travaille avec les positions des caractères au lieu d’un délimiteur. Cet exemple conserve les huit premiers caractères de chaque ligne.</p>\n<h3>Lire un flux</h3>\n<pre><code>cat normalisation/espaces | tr -s &#39; &#39; | cut -d&#39; &#39; -f3</code></pre>\n<p><code>cut</code> reçoit ici un texte dont les espaces ont été normalisés par <code>tr</code>. Il extrait ensuite le troisième champ.</p>\n<h2>Exemple commenté</h2>\n<p>Imagine que <code>colonnes/fiche</code> contient trois champs séparés par <code>:</code>.</p>\n<pre><code>$ cat colonnes/fiche\nalice:admin:FLAG{ALPHA}\nbob:users:FLAG{BETA}\n$ cut -d: -f3 colonnes/fiche\nFLAG{ALPHA}\nFLAG{BETA}</code></pre>\n<p><code>-d:</code> découpe chaque ligne aux deux-points. <code>-f3</code> conserve uniquement le troisième champ.</p>\n<h2>Points de vigilance</h2>\n<p>Le délimiteur de <code>cut</code> est un caractère précis. Une virgule, un deux-points et un point-virgule ne produisent pas le même découpage.</p>\n<p>Des séparateurs consécutifs créent des champs vides. Normalise d’abord les espaces irréguliers avec <code>tr -s &#39; &#39;</code> si nécessaire.</p>\n<p><code>cut</code> n’effectue ni tri ni comptage. Transmets sa sortie à <code>sort</code>, <code>uniq</code> ou <code>wc</code> pour poursuivre le traitement.</p>"
  },
  {
    "id": "diff",
    "label": "diff",
    "group": "Texte et flux",
    "summary": "Compare deux fichiers ligne par ligne et décrit leurs différences.",
    "html": "<h2>Comprendre</h2>\n<p><code>diff</code> compare deux fichiers texte ligne par ligne. La commande indique ce qui a été supprimé, ajouté ou remplacé pour passer du premier fichier au second.</p>\n<p>L’ordre des arguments est important. Le premier représente l’ancienne version et le second la nouvelle version dans les exemples du cours. <code>diff</code> décrit les écarts sans modifier ni fusionner les fichiers.</p>\n<h2>Commandes et options</h2>\n<h3>Comparer deux fichiers</h3>\n<pre><code>diff config/service-ancien config/service-actuel</code></pre>\n<p>Sans option, <code>diff</code> utilise son format historique. Une sortie vide signifie que les fichiers sont identiques pour cette comparaison.</p>\n<h3>Utiliser le format unifié</h3>\n<pre><code>diff -u config/service-ancien config/service-actuel</code></pre>\n<p>L’option <code>-u</code> produit un résultat plus lisible avec du contexte autour des changements. Les lignes précédées de <code>-</code> appartiennent à l’ancienne version. Les lignes précédées de <code>+</code> appartiennent à la nouvelle version. Une ligne commençant par un espace sert de contexte.</p>\n<h3>Parcourir une longue comparaison</h3>\n<pre><code>diff -u config/volume-ancien config/volume-actuel | less</code></pre>\n<p>Le pipe envoie la comparaison à <code>less</code>. Tu peux alors rechercher une section avec <code>/mot</code> et quitter avec <code>q</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ diff -u config/service-ancien config/service-actuel\n--- config/service-ancien\n+++ config/service-actuel\n@@ -1,2 +1,3 @@\n port=8080\n+FLAG{EXEMPLE}\n mode=actif</code></pre>\n<p>Les deux lignes d’en-tête nomment les fichiers comparés. La ligne <code>FLAG{EXEMPLE}</code> commence par <code>+</code>, elle existe donc dans <code>service-actuel</code> mais pas dans <code>service-ancien</code>.</p>\n<h2>Points de vigilance</h2>\n<p>Inverse les deux arguments et les ajouts deviennent des suppressions. Garde toujours en tête l’ordre ancienne version puis nouvelle version.</p>\n<p>Les lignes <code>---</code> et <code>+++</code> sont les en-têtes du format unifié. Elles ne représentent pas directement du contenu supprimé ou ajouté.</p>\n<p>Un caractère <code>+</code> ou <code>-</code> présent plus loin dans le contenu n’est pas forcément un marqueur. Le marqueur de <code>diff</code> occupe la première colonne de la ligne.</p>"
  },
  {
    "id": "du-df",
    "label": "du / df",
    "group": "Système et administration",
    "summary": "du mesure les fichiers et df affiche l’espace disponible sur les systèmes de fichiers.",
    "html": "<h2>Comprendre</h2>\n<p><code>du</code> mesure les fichiers et <code>df</code> affiche l’espace disponible sur les systèmes de fichiers.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>du -sh dossier  |  df -h</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>du -sh donnees</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Utilise <code>df</code> pour diagnostiquer un disque plein et <code>du</code> pour localiser un gros dossier.</p>"
  },
  {
    "id": "file",
    "label": "file",
    "group": "Texte et flux",
    "summary": "Identifie le type probable d’un fichier à partir de son contenu.",
    "html": "<h2>Comprendre</h2>\n<p><code>file</code> examine le contenu d’un fichier et cherche des signatures connues. Il peut reconnaître du texte, une image, une archive, un exécutable, une page HTML ou un script même si le nom ne possède aucune extension.</p>\n<p>La commande fournit une description probable. Elle ne modifie pas le fichier et ne garantit pas que son contenu est sûr.</p>\n<h2>Commandes et options</h2>\n<h3>Identifier un fichier</h3>\n<pre><code>file objets/document</code></pre>\n<p>Le résultat contient le nom du fichier puis une description de son type.</p>\n<h3>Examiner plusieurs fichiers</h3>\n<pre><code>file objets/*</code></pre>\n<p>Le joker <code>*</code> est développé par le shell. <code>file</code> examine chaque entrée visible correspondante et affiche une ligne de résultat par fichier.</p>\n<h3>Afficher seulement la description</h3>\n<pre><code>file -b objets/document</code></pre>\n<p>L’option <code>-b</code> retire le nom du fichier au début de la sortie. Seule la description reste affichée.</p>\n<h3>Examiner ensuite un fichier texte</h3>\n<pre><code>file objets/document\ncat objets/document</code></pre>\n<p>Après avoir confirmé que le fichier contient du texte, <code>cat</code> ou <code>less</code> permet de le lire.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ file objets/*\nobjets/page:      HTML document, Unicode text, UTF-8 text\nobjets/script:    Bourne-Again shell script, ASCII text executable\nobjets/document:  Unicode text, UTF-8 text</code></pre>\n<p>Le nom et l’extension ne sont pas nécessaires pour distinguer les trois contenus. <code>document</code> est le fichier texte qui n’est décrit ni comme une page HTML ni comme un script.</p>\n<h2>Points de vigilance</h2>\n<p><code>file</code> identifie un format probable. Il ne réalise pas une analyse antivirus et ne prouve pas qu’un fichier peut être exécuté sans danger.</p>\n<p>Une extension peut être trompeuse. Renommer un fichier en <code>.txt</code> ne transforme pas son contenu.</p>\n<p>Le joker <code>*</code> n’inclut pas les noms cachés. Il peut aussi désigner des dossiers, que <code>file</code> décrira comme tels.</p>"
  },
  {
    "id": "find",
    "label": "find",
    "group": "Fichiers et droits",
    "summary": "Cherche des fichiers dans une arborescence.",
    "html": "<h2>Comprendre</h2>\n<p>Cherche des fichiers dans une arborescence.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>find depart -type f -name &quot;motif&quot;</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>find donnees -type f -name &quot;*.log&quot;</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Mets le motif entre guillemets pour le laisser à <code>find</code>.</p>"
  },
  {
    "id": "grep",
    "label": "grep",
    "group": "Texte et flux",
    "summary": "Affiche les lignes d’un fichier qui correspondent à un motif.",
    "html": "<h2>Comprendre</h2>\n<p>Affiche les lignes d’un fichier qui correspondent à un motif.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>grep &quot;motif&quot; fichier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>grep &quot;ERREUR&quot; donnees/logs/serveur.log</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>-n</code> ajoute les numéros de ligne. <code>-E</code> active les expressions régulières étendues.</p>"
  },
  {
    "id": "head",
    "label": "head",
    "group": "Texte et flux",
    "summary": "Affiche les premières lignes d’un fichier ou d’un flux.",
    "html": "<h2>Comprendre</h2>\n<p><code>head</code> sélectionne le début d’un fichier ou d’un flux. La commande affiche dix lignes par défaut et écrit le résultat sur la sortie standard.</p>\n<p>Elle ne modifie pas la source. Elle permet d’inspecter rapidement la structure d’un fichier ou de conserver un nombre précis de premières lignes dans un pipeline.</p>\n<h2>Commandes et options</h2>\n<h3>Afficher les dix premières lignes</h3>\n<pre><code>head journaux/rotation</code></pre>\n<p>Sans option, <code>head</code> affiche les dix premières lignes. Si le fichier en contient moins, toutes ses lignes sont affichées.</p>\n<h3>Choisir le nombre de lignes</h3>\n<pre><code>head -n 1 journaux/rotation\nhead -n 11 journaux/rotation</code></pre>\n<p>L’option <code>-n</code> fixe le nombre de lignes conservées. <code>-n 1</code> isole la première ligne. <code>-n 11</code> affiche les onze premières lignes dans leur ordre d’origine.</p>\n<h3>Lire un flux</h3>\n<pre><code>cat flux/chronologie | head -n 3</code></pre>\n<p>Sans nom de fichier, <code>head</code> peut lire l’entrée standard. Dans cet exemple, il conserve les trois premières lignes produites par <code>cat</code>.</p>\n<h3>Isoler une ligne avec tail</h3>\n<pre><code>head -n 8 flux/chronologie | tail -n 1</code></pre>\n<p><code>head</code> garde les huit premières lignes. <code>tail -n 1</code> conserve ensuite la dernière de ce groupe, ce qui isole la ligne 8.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ head -n 3 journaux/rotation\nligne 1  démarrage\nligne 2  vérification\nligne 3  service prêt</code></pre>\n<p>La sortie contient exactement les trois premières lignes. Les lignes suivantes restent dans le fichier mais ne sont pas affichées.</p>\n<h2>Points de vigilance</h2>\n<p><code>head</code> sélectionne le début du flux qu’il reçoit. Placé après une autre commande, il ne connaît pas le fichier d’origine.</p>\n<p><code>head -n 11</code> affiche les onze premières lignes. Il n’affiche pas uniquement la ligne 11. Ajoute <code>tail -n 1</code> pour isoler cette ligne.</p>\n<p>Évite de rediriger le résultat vers le fichier source avec <code>&gt;</code>. Le shell viderait le fichier avant sa lecture.</p>"
  },
  {
    "id": "ip-ss",
    "label": "ip / ss",
    "group": "Système et administration",
    "summary": "ip inspecte les interfaces et les routes tandis que ss montre les sockets.",
    "html": "<h2>Comprendre</h2>\n<p><code>ip</code> inspecte les interfaces et les routes tandis que <code>ss</code> montre les sockets.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>ip -br address  |  ss -lnt</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>ss -lnt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Un service actif peut ne pas écouter sur le port ou l’interface attendus.</p>"
  },
  {
    "id": "jobs",
    "label": "Ctrl-Z / bg / fg",
    "group": "Système et administration",
    "summary": "Suspend puis déplace un job entre premier plan et arrière-plan.",
    "html": "<h2>Comprendre</h2>\n<p>Suspend puis déplace un job entre premier plan et arrière-plan.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>Ctrl-Z  puis  bg  ou  fg</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>sleep 300  # puis Ctrl-Z, bg, fg</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>Ctrl-Z</code> suspend le job et <code>Ctrl-C</code> l’interrompt. Un job en arrière-plan peut continuer à travailler.</p>"
  },
  {
    "id": "journalctl",
    "label": "journalctl",
    "group": "Système et administration",
    "summary": "Consulte le journal des services gérés par systemd.",
    "html": "<h2>Comprendre</h2>\n<p>Consulte le journal des services gérés par systemd.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>journalctl -u unite -n 20 --no-pager</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>journalctl -u sshd -n 20 --no-pager</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Les droits du compte peuvent limiter les entrées visibles.</p>"
  },
  {
    "id": "less",
    "label": "less",
    "group": "Texte et flux",
    "summary": "Parcourt et recherche un texte long sans modifier le fichier.",
    "html": "<h2>Comprendre</h2>\n<p><code>less</code> ouvre un fichier dans un lecteur interactif appelé <strong>pager</strong>. Le texte est affiché écran par écran au lieu de défiler entièrement dans le terminal.</p>\n<p>La commande permet de se déplacer et de rechercher une expression sans modifier le fichier. Elle convient aux longs documents, aux journaux et aux sorties volumineuses d’autres commandes.</p>\n<h2>Commandes et options</h2>\n<h3>Ouvrir un fichier</h3>\n<pre><code>less manuel/guide</code></pre>\n<p>Le fichier s’ouvre dans le lecteur. L’invite de commande réapparaît seulement après avoir quitté <code>less</code>.</p>\n<h3>Se déplacer</h3>\n<p>Les flèches parcourent le texte ligne par ligne. La barre d’espace ou <code>Page suivante</code> avance d’un écran. <code>Page précédente</code> remonte d’un écran.</p>\n<p>La touche <code>g</code> rejoint le début du document. La touche <code>G</code> avec une majuscule rejoint sa fin.</p>\n<h3>Rechercher vers l’avant</h3>\n<p>Saisis <code>/</code> suivi du texte recherché puis appuie sur Entrée. <code>/PROCÉDURE ACTIVE</code> rejoint la prochaine occurrence de cette expression.</p>\n<p>La touche <code>n</code> passe au résultat suivant. La touche <code>N</code> revient au résultat précédent.</p>\n<h3>Rechercher vers l’arrière</h3>\n<p>Saisis <code>?</code> suivi du texte recherché pour chercher vers le début du document. Cette méthode est pratique après avoir rejoint la fin avec <code>G</code>.</p>\n<h3>Quitter</h3>\n<p>Appuie sur <code>q</code> pour fermer le lecteur et revenir au terminal.</p>\n<h3>Lire la sortie d’une commande</h3>\n<pre><code>diff -u ancien actuel | less</code></pre>\n<p><code>less</code> peut recevoir un flux depuis un pipe. Ici, la comparaison produite par <code>diff</code> devient consultable écran par écran.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ less manuel/guide\nPROCÉDURE ARCHIVÉE\nNe plus appliquer cette procédure.\n\nPROCÉDURE ACTIVE\nFLAG{EXEMPLE}</code></pre>\n<p>Après l’ouverture, saisis <code>/PROCÉDURE ACTIVE</code> pour rejoindre directement la bonne section. Utilise <code>n</code> si le texte apparaît plusieurs fois puis <code>q</code> pour quitter.</p>\n<h2>Points de vigilance</h2>\n<p>Les touches <code>/</code>, <code>?</code>, <code>n</code>, <code>G</code> et <code>q</code> sont utilisées dans le lecteur. Elles ne doivent pas être saisies après l’invite <code>$</code>.</p>\n<p><code>less</code> sert à consulter. Il n’enregistre pas de modification dans le fichier.</p>\n<p>Une recherche tient compte du texte exact. Vérifie la casse et les accents si aucun résultat n’apparaît.</p>"
  },
  {
    "id": "ln",
    "label": "ln -s",
    "group": "Fichiers et droits",
    "summary": "Crée un lien symbolique vers un autre chemin.",
    "html": "<h2>Comprendre</h2>\n<p>Crée un lien symbolique vers un autre chemin.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>ln -s cible lien</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>ln -s ../donnees/notes/consignes.txt travail/consignes-lien.txt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Le chemin cible relatif est calculé depuis le dossier du lien.</p>"
  },
  {
    "id": "ls",
    "label": "ls",
    "group": "Navigation",
    "summary": "Liste le contenu d’un dossier et affiche les entrées cachées ou leurs détails.",
    "html": "<h2>Comprendre</h2>\n<p><code>ls</code> affiche les entrées contenues dans un dossier. Une entrée peut être un fichier, un dossier ou un lien. Sans chemin, la commande examine le dossier courant.</p>\n<p>Contrairement à <code>cd</code>, <code>ls</code> ne change jamais le dossier courant. Tu peux examiner un autre emplacement avec <code>ls chemin</code> puis lancer <code>pwd</code>. Le chemin affiché par <code>pwd</code> reste identique.</p>\n<p>Par défaut, les noms commençant par un point comme <code>.secret</code> sont masqués. Ils existent bien sur le système. Le point placé au début du nom est une convention utilisée par Linux pour identifier les entrées cachées.</p>\n<h2>Commandes et options</h2>\n<h3>Lister le dossier courant</h3>\n<pre><code>ls</code></pre>\n<p><code>ls</code> affiche les entrées visibles du dossier courant. La commande montre leurs noms sans entrer dans les sous-dossiers et sans modifier les fichiers.</p>\n<p>Si le terminal n’affiche rien, le dossier ne contient aucune entrée visible. Il peut cependant contenir des entrées cachées.</p>\n<h3>Lister un autre dossier</h3>\n<pre><code>ls rep1\nls rep1/rep2</code></pre>\n<p><code>ls rep1</code> affiche les entrées visibles de <code>rep1</code> sans quitter le dossier courant. Le second exemple suit le chemin relatif <code>rep1/rep2</code> et affiche le contenu visible de <code>rep2</code>.</p>\n<p>Le chemin peut aussi être absolu. <code>ls /etc</code> examine toujours le dossier <code>/etc</code>, quelle que soit la position actuelle.</p>\n<h3>Afficher les détails</h3>\n<pre><code>ls -l\nls -l rep1</code></pre>\n<p>L’option <code>-l</code> utilise un format long. Chaque entrée occupe une ligne qui présente les informations suivantes.</p>\n<ul><li>Le type de l’entrée et ses droits apparaissent au début de la ligne. Le premier caractère vaut notamment <code>d</code> pour un dossier et <code>-</code> pour un fichier ordinaire.</li><li>Le nombre de liens physiques vers l’inode est affiché après les droits. Un inode contient les métadonnées qui identifient l’entrée sur le système de fichiers.</li><li>Le propriétaire et le groupe sont indiqués dans les colonnes suivantes.</li><li>La taille est affichée en octets.</li><li>La date de dernière modification précède le nom.</li></ul>\n<p>La ligne <code>total</code> placée au début du résultat représente l’espace réellement alloué aux entrées listées. Sur un système GNU/Linux courant, cette valeur est généralement exprimée en blocs de 1 Kio. Elle ne donne ni le nombre d’entrées ni la simple somme des tailles affichées.</p>\n<h3>Afficher les entrées cachées</h3>\n<pre><code>ls -a\nls -a rep1</code></pre>\n<p>L’option <code>-a</code> affiche toutes les entrées, y compris celles dont le nom commence par un point. Le résultat contient aussi <code>.</code> pour le dossier courant et <code>..</code> pour son dossier parent. Avec <code>ls -a rep1</code>, <code>.</code> désigne <code>rep1</code>, même si le dossier courant du terminal ne change pas.</p>\n<h3>Combiner les détails et les entrées cachées</h3>\n<pre><code>ls -la\nls -la rep1</code></pre>\n<p>Les options courtes peuvent être regroupées. <code>ls -la</code> combine donc le format long de <code>-l</code> avec l’affichage complet de <code>-a</code>. La forme <code>ls -al</code> produit le même résultat.</p>\n<h2>Exemple commenté</h2>\n<p>Imagine que tu te trouves dans <code>/home/alice/atelier</code>. Ce dossier contient <code>notes.txt</code> et le dossier <code>rep1</code>. <code>rep1</code> contient le fichier visible <code>config.txt</code> et le fichier caché <code>.secret</code>.</p>\n<pre><code>$ pwd\n/home/alice/atelier\n$ ls\nnotes.txt  rep1\n$ ls rep1\nconfig.txt\n$ ls -a rep1\n.  ..  .secret  config.txt\n$ ls -l rep1\ntotal 4\n-rw-r--r-- 1 alice alice 128 24 sept. 10:30 config.txt\n$ ls -la rep1\ntotal 16\ndrwxr-xr-x 2 alice alice 4096 24 sept. 10:30 .\ndrwxr-xr-x 3 alice alice 4096 24 sept. 10:20 ..\n-rw------- 1 alice alice   42 24 sept. 10:25 .secret\n-rw-r--r-- 1 alice alice  128 24 sept. 10:30 config.txt</code></pre>\n<p>La première commande <code>ls</code> affiche les deux entrées visibles du dossier courant. <code>ls rep1</code> examine ensuite un autre dossier sans utiliser <code>cd</code>. Le fichier <code>.secret</code> devient visible uniquement avec <code>-a</code>.</p>\n<p><code>ls -l rep1</code> ajoute les détails mais ne montre toujours pas <code>.secret</code>. Il faut combiner les deux options avec <code>ls -la rep1</code> pour obtenir les détails de toutes les entrées.</p>\n<h2>Points de vigilance</h2>\n<p><code>ls -l rep1</code> affiche le contenu détaillé de <code>rep1</code>, pas les informations du dossier <code>rep1</code> lui-même. La commande <code>ls -ld rep1</code> permet d’examiner le dossier sans lister son contenu.</p>\n<p>Un dossier peut sembler vide avec <code>ls</code> tout en contenant des entrées cachées. Utilise <code>ls -a</code> pour le vérifier.</p>\n<p>Linux distingue les majuscules et les minuscules. <code>ls Documents</code> et <code>ls documents</code> visent donc deux dossiers différents.</p>\n<p>Un chemin contenant des espaces doit être protégé. Utilise par exemple <code>ls &quot;Mes documents&quot;</code> ou <code>ls Mes\\ documents</code>.</p>"
  },
  {
    "id": "man",
    "label": "man",
    "group": "Navigation",
    "summary": "Consulte le manuel local d’une commande et recherche une information précise.",
    "html": "<h2>Comprendre</h2>\n<p><code>man</code>, abréviation de <strong>manual</strong>, ouvre la documentation installée sur la machine Linux. Il permet de comprendre une commande sans quitter le terminal et sans effectuer de recherche sur le Web.</p>\n<p>Une page de manuel décrit généralement le rôle de la commande, sa syntaxe, ses options, ses fichiers associés et parfois des exemples. Consulter une page ne lance pas la commande présentée et ne modifie aucun fichier.</p>\n<p>Les pages sont affichées dans un lecteur qui permet de se déplacer, de rechercher un mot et de quitter la documentation.</p>\n<h2>Commandes et options</h2>\n<h3>Ouvrir la page d’une commande</h3>\n<pre><code>man ls\nman pwd</code></pre>\n<p>La forme générale est <code>man commande</code>. <code>man ls</code> ouvre la documentation de <code>ls</code> et <code>man pwd</code> celle de <code>pwd</code>.</p>\n<p>Les rubriques <code>NAME</code>, <code>SYNOPSIS</code>, <code>DESCRIPTION</code> et <code>OPTIONS</code> sont particulièrement utiles. <code>SYNOPSIS</code> montre la manière correcte d’écrire la commande. Les éléments placés entre crochets sont généralement facultatifs.</p>\n<h3>Se déplacer dans une page</h3>\n<p>Utilise les flèches ou les touches <code>Page précédente</code> et <code>Page suivante</code> pour parcourir le texte. La barre d’espace avance également d’un écran.</p>\n<p>Ces touches sont utilisées pendant que la page est ouverte. Il ne faut pas les saisir après l’invite <code>$</code>.</p>\n<h3>Rechercher dans la page</h3>\n<p>Une fois le manuel ouvert, saisis <code>/</code> suivi du mot recherché puis appuie sur Entrée. Par exemple, <code>/hidden</code> cherche le mot <code>hidden</code> dans la page de <code>ls</code>.</p>\n<p>La touche <code>n</code> rejoint le résultat suivant. La touche <code>N</code> avec une majuscule revient au résultat précédent.</p>\n<h3>Quitter le manuel</h3>\n<p>Appuie sur <code>q</code> pour fermer la page et revenir à l’invite de commande.</p>\n<h3>Chercher une page par sujet</h3>\n<pre><code>man -k dossier\napropos dossier</code></pre>\n<p><code>man -k mot</code> recherche ce mot dans les noms et les descriptions courtes des pages. <code>apropos mot</code> effectue la même recherche. Ces commandes sont utiles lorsque tu connais le sujet, mais pas encore le nom de la commande.</p>\n<h3>Choisir une section précise</h3>\n<pre><code>man 1 passwd\nman 5 passwd</code></pre>\n<p>Le manuel est réparti en sections. La section <code>1</code> documente les commandes utilisables dans le terminal. La section <code>5</code> documente les formats de fichiers. Un même nom peut exister dans plusieurs sections. Ici, <code>man 1 passwd</code> décrit la commande tandis que <code>man 5 passwd</code> décrit le fichier <code>/etc/passwd</code>.</p>\n<h3>Utiliser une aide plus courte</h3>\n<pre><code>ls --help\nhelp cd</code></pre>\n<p>De nombreuses commandes acceptent <code>--help</code> pour afficher un résumé rapide. Certaines commandes intégrées au shell, comme <code>cd</code>, sont plutôt documentées avec <code>help cd</code>. Ces solutions sont utiles si aucune page de manuel n’est installée.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ man ls\nLS(1)                     Commandes de l’utilisateur                    LS(1)\nNAME\n       ls - list directory contents\nSYNOPSIS\n       ls [OPTION]... [FILE]...</code></pre>\n<p>L’en-tête indique le nom <code>ls</code> et la section <code>1</code>. La rubrique <code>NAME</code> résume le rôle de la commande. <code>SYNOPSIS</code> montre que <code>ls</code> accepte des options et un ou plusieurs chemins. Les crochets indiquent que ces éléments sont facultatifs.</p>\n<p>Depuis cette page, saisis <code>/all</code> pour rechercher le mot <code>all</code>, utilise <code>n</code> pour parcourir les résultats puis appuie sur <code>q</code> pour revenir au terminal.</p>\n<h2>Points de vigilance</h2>\n<p><code>/mot</code>, <code>n</code> et <code>q</code> sont des actions du lecteur de manuel. Elles ne fonctionnent ainsi que lorsque la page est ouverte.</p>\n<p>Le texte et les exemples peuvent être en anglais selon les pages installées sur la machine.</p>\n<p>Une page peut manquer si la documentation correspondante n’est pas installée. Essaie alors <code>commande --help</code> ou <code>help commande</code> pour une commande intégrée au shell.</p>\n<p>Le manuel décrit toutes les possibilités d’une commande, y compris des options avancées ou risquées. Lis leur description avant de les utiliser.</p>"
  },
  {
    "id": "mkdir",
    "label": "mkdir",
    "group": "Fichiers et droits",
    "summary": "Crée un dossier.",
    "html": "<h2>Comprendre</h2>\n<p>Crée un dossier.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>mkdir dossier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>mkdir travail/rapports</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Le parent doit exister, sauf si tu utilises <code>mkdir -p</code>.</p>"
  },
  {
    "id": "mv",
    "label": "mv",
    "group": "Fichiers et droits",
    "summary": "Déplace ou renomme un fichier ou dossier.",
    "html": "<h2>Comprendre</h2>\n<p>Déplace ou renomme un fichier ou dossier.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>mv source destination</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>mv travail/bilan.txt travail/synthese.txt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Contrôle le dossier cible. Un déplacement peut être silencieux.</p>"
  },
  {
    "id": "pipe",
    "label": "| (tuyau)",
    "group": "Texte et flux",
    "summary": "Transmet la sortie standard d’une commande à l’entrée standard d’une autre.",
    "html": "<h2>Comprendre</h2>\n<p>Le caractère <code>|</code>, appelé <strong>pipe</strong> ou <strong>tuyau</strong>, relie deux commandes. La sortie standard de la commande placée à gauche devient l’entrée standard de celle placée à droite.</p>\n<p>Chaque commande accomplit une étape simple. Leur enchaînement construit un traitement plus précis sans créer de fichier temporaire.</p>\n<h2>Commandes et options</h2>\n<h3>Relier deux commandes</h3>\n<pre><code>cat flux/chronologie | head -n 1</code></pre>\n<p><code>cat</code> produit le contenu du fichier. Le pipe transmet ce contenu à <code>head</code>, qui conserve seulement la première ligne.</p>\n<h3>Isoler une ligne précise</h3>\n<pre><code>head -n 8 flux/chronologie | tail -n 1</code></pre>\n<p>La première commande garde les huit premières lignes. La seconde garde la dernière ligne reçue. Le résultat correspond à la ligne 8 du fichier.</p>\n<h3>Enchaîner plusieurs traitements</h3>\n<pre><code>cut -d: -f2 pipeline/incidents | tr &#39;[:lower:]&#39; &#39;[:upper:]&#39; | sort | uniq -c</code></pre>\n<p>Le flux passe de gauche à droite. <code>cut</code> extrait une colonne, <code>tr</code> normalise la casse, <code>sort</code> rapproche les valeurs identiques et <code>uniq -c</code> compte leurs occurrences.</p>\n<h3>Parcourir une sortie longue</h3>\n<pre><code>diff -u ancien actuel | less</code></pre>\n<p>La sortie de <code>diff</code> n’est pas affichée directement. Elle devient le document consulté dans <code>less</code>.</p>\n<h2>Exemple commenté</h2>\n<p>Imagine que <code>flux/chronologie</code> contient douze lignes.</p>\n<pre><code>$ head -n 3 flux/chronologie\nétape 1\nétape 2\nFLAG{EXEMPLE}\n$ head -n 3 flux/chronologie | tail -n 1\nFLAG{EXEMPLE}</code></pre>\n<p>La première commande montre trois lignes. Dans le pipeline, <code>tail -n 1</code> reçoit uniquement ces trois lignes et conserve la dernière. Le fichier source reste inchangé.</p>\n<h2>Points de vigilance</h2>\n<p>Construis un pipeline progressivement. Vérifie d’abord la sortie de chaque commande avant d’ajouter l’étape suivante.</p>\n<p>Le pipe transmet seulement la sortie standard. Les messages d’erreur utilisent normalement un autre flux et ne sont pas transmis automatiquement.</p>\n<p>Certaines commandes comme <code>cd</code> doivent modifier le shell courant. Les placer dans un pipeline ne permet pas de conserver ce changement dans le terminal principal.</p>"
  },
  {
    "id": "ps-kill",
    "label": "ps / kill",
    "group": "Système et administration",
    "summary": "ps observe les processus et kill envoie un signal à un PID.",
    "html": "<h2>Comprendre</h2>\n<p><code>ps</code> observe les processus et <code>kill</code> envoie un signal à un PID.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>ps -p PID  |  kill PID</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>ps -p 1234 -o pid,comm</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Vérifie le PID et la commande avant de terminer un processus.</p>"
  },
  {
    "id": "pwd",
    "label": "pwd",
    "group": "Navigation",
    "summary": "Affiche le chemin absolu du dossier courant et confirme la position du terminal.",
    "html": "<h2>Comprendre</h2>\n<p><code>pwd</code>, abréviation de <strong>print working directory</strong>, affiche le chemin absolu du dossier courant. Ce chemin commence toujours par la racine <code>/</code> et décrit chaque dossier traversé pour atteindre la position du terminal.</p>\n<p>Connaître cette position permet de comprendre comment les chemins relatifs seront interprétés. <code>pwd</code> est particulièrement utile avant une commande qui copie, déplace, modifie ou supprime des fichiers.</p>\n<p>La commande affiche une information. Elle ne change pas de dossier et ne modifie aucun fichier.</p>\n<h2>Commandes et options</h2>\n<h3>Afficher le chemin courant</h3>\n<pre><code>pwd</code></pre>\n<p><code>pwd</code> affiche un seul chemin absolu. Par exemple, <code>/home/alice/atelier</code> indique que le terminal se trouve dans <code>atelier</code>, lui-même placé dans le dossier personnel d’<code>alice</code> sous <code>/home</code>.</p>\n<p>Le shell conserve aussi le chemin courant dans la variable <code>$PWD</code>. Cette variable est mise à jour après chaque changement de dossier réussi et peut être utilisée dans un script.</p>\n<h3>Afficher le chemin logique</h3>\n<pre><code>pwd -L</code></pre>\n<p><code>pwd -L</code> affiche le chemin logique. Il conserve les liens symboliques présents dans le chemin utilisé pour atteindre le dossier. C’est généralement le comportement appliqué par défaut.</p>\n<h3>Afficher le chemin physique</h3>\n<pre><code>pwd -P</code></pre>\n<p><code>pwd -P</code> affiche le chemin physique réel. Les liens symboliques sont remplacés par les dossiers vers lesquels ils pointent.</p>\n<p>Pour débuter, <code>pwd</code> sans option suffit dans la plupart des situations. La différence entre <code>-L</code> et <code>-P</code> devient utile lorsqu’un parcours contient des liens symboliques.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ pwd\n/home/alice/atelier</code></pre>\n<p>Le premier <code>/</code> représente la racine du système. <code>home</code> contient les dossiers personnels, <code>alice</code> est le dossier personnel de l’utilisatrice et <code>atelier</code> est le dossier courant.</p>\n<p>Le terminal peut parfois afficher seulement <code>~/atelier</code> dans son invite. <code>pwd</code> fournit la forme absolue complète <code>/home/alice/atelier</code>.</p>\n<p>Avec un lien symbolique nommé <code>raccourci</code> qui pointe vers <code>/srv/projet</code>, les deux options peuvent produire des résultats différents.</p>\n<pre><code>$ pwd -L\n/home/alice/raccourci\n$ pwd -P\n/srv/projet</code></pre>\n<h2>Points de vigilance</h2>\n<p>L’invite du terminal n’affiche pas toujours le chemin complet. Utilise <code>pwd</code> plutôt que de te fier uniquement à cette invite.</p>\n<p><code>pwd</code> n’accepte pas un nom de dossier à examiner. <code>pwd rep1</code> ne demande pas le chemin absolu de <code>rep1</code>. La commande décrit uniquement le dossier courant.</p>\n<p>Le caractère <code>~</code> peut représenter le dossier personnel dans une commande, mais <code>pwd</code> affiche son véritable chemin absolu comme <code>/home/alice</code>.</p>\n<p>Avant une commande qui écrit, déplace ou supprime des données, vérifie ta position avec <code>pwd</code>.</p>"
  },
  {
    "id": "redirections",
    "label": ">  >>  2>",
    "group": "Texte et flux",
    "summary": "Envoie la sortie normale ou les erreurs vers un fichier.",
    "html": "<h2>Comprendre</h2>\n<p>Envoie la sortie normale ou les erreurs vers un fichier.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>commande &gt; fichier  |  commande &gt;&gt; fichier  |  commande 2&gt; erreurs</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>grep ERREUR journal.log &gt; erreurs.txt</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>&gt;</code> remplace le contenu, <code>&gt;&gt;</code> ajoute à la fin et <code>2&gt;</code> capture les erreurs.</p>"
  },
  {
    "id": "rm",
    "label": "rm",
    "group": "Fichiers et droits",
    "summary": "Supprime le fichier indiqué.",
    "html": "<h2>Comprendre</h2>\n<p>Supprime le fichier indiqué.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>rm fichier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>rm travail/brouillon.tmp</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Il n’y a pas de corbeille habituelle. Prévisualise les jokers avec <code>echo</code> ou <code>ls</code>.</p>"
  },
  {
    "id": "rpm-dnf",
    "label": "rpm / dnf",
    "group": "Système et administration",
    "summary": "Sur les distributions basées sur RPM, rpm interroge les paquets installés et dnf gère les dépôts.",
    "html": "<h2>Comprendre</h2>\n<p>Sur les distributions basées sur RPM, rpm interroge les paquets installés et dnf gère les dépôts.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>rpm -q paquet  |  dnf repolist --enabled</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>rpm -q bash</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Ces commandes d’inspection ne demandent pas de modifier les paquets.</p>"
  },
  {
    "id": "sed",
    "label": "sed",
    "group": "Texte et flux",
    "summary": "Transforme un flux de texte, notamment par substitution.",
    "html": "<h2>Comprendre</h2>\n<p>Transforme un flux de texte, notamment par substitution.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>sed &quot;s/ancien/nouveau/&quot; fichier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>sed &quot;s/^MODE=debug$/MODE=production/&quot; app.conf</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p>Sans <code>-i</code>, le fichier source reste inchangé. Redirige la sortie vers une copie.</p>"
  },
  {
    "id": "selinux",
    "label": "getenforce",
    "group": "Système et administration",
    "summary": "Affiche le mode courant de SELinux.",
    "html": "<h2>Comprendre</h2>\n<p>Affiche le mode courant de SELinux.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>getenforce</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>getenforce</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>Enforcing</code> applique les règles. <code>Permissive</code> journalise sans bloquer. <code>Disabled</code> désactive SELinux.</p>"
  },
  {
    "id": "sort",
    "label": "sort",
    "group": "Texte et flux",
    "summary": "Trie des lignes entières ou selon une clé textuelle ou numérique.",
    "html": "<h2>Comprendre</h2>\n<p><code>sort</code> reçoit des lignes et les affiche dans un nouvel ordre. Par défaut, la comparaison est textuelle et dépend des règles de langue configurées sur la machine.</p>\n<p>La commande n’écrit pas dans le fichier source. Elle produit un résultat trié sur la sortie standard et prépare souvent les données pour <code>uniq</code>.</p>\n<h2>Commandes et options</h2>\n<h3>Trier des lignes</h3>\n<pre><code>sort tri/noms</code></pre>\n<p>Sans option, <code>sort</code> compare le texte des lignes. Les nombres sont alors traités comme des suites de caractères.</p>\n<h3>Trier numériquement</h3>\n<pre><code>sort -n tri/scores</code></pre>\n<p>L’option <code>-n</code> compare les valeurs comme des nombres. Elle évite de considérer <code>12</code> comme inférieur à <code>9</code> à cause de son premier caractère.</p>\n<h3>Inverser l’ordre</h3>\n<pre><code>sort -r tri/noms\nsort -nr tri/scores</code></pre>\n<p><code>-r</code> inverse le résultat. Les options peuvent être regroupées. <code>-nr</code> produit donc un tri numérique décroissant.</p>\n<h3>Trier selon un champ</h3>\n<pre><code>sort -t: -k2,2n tri/charges</code></pre>\n<p><code>-t:</code> choisit <code>:</code> comme séparateur de champs. <code>-k2,2</code> limite la clé au deuxième champ. Le suffixe <code>n</code> demande une comparaison numérique de cette clé.</p>\n<h3>Préparer les données pour uniq</h3>\n<pre><code>sort doublons/brut | uniq -d</code></pre>\n<p>Le tri rapproche les lignes identiques. <code>uniq -d</code> peut alors détecter tous les doublons du flux.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ cat tri/scores\n9 FLAG{NEUF}\n12 FLAG{DOUZE}\n3 FLAG{TROIS}\n$ sort tri/scores\n12 FLAG{DOUZE}\n3 FLAG{TROIS}\n9 FLAG{NEUF}\n$ sort -n tri/scores\n3 FLAG{TROIS}\n9 FLAG{NEUF}\n12 FLAG{DOUZE}</code></pre>\n<p>Le premier tri compare du texte et place <code>12</code> avant <code>9</code>. L’option <code>-n</code> rétablit l’ordre numérique attendu.</p>\n<h2>Points de vigilance</h2>\n<p>Un tri textuel et un tri numérique peuvent produire des résultats très différents. Vérifie toujours la nature de la clé.</p>\n<p><code>-k2</code> peut continuer la comparaison jusqu’à la fin de la ligne. Utilise <code>-k2,2</code> pour limiter précisément la clé au deuxième champ.</p>\n<p><code>sort</code> ne modifie pas le fichier par défaut. Une redirection est nécessaire pour enregistrer le résultat dans un autre fichier.</p>"
  },
  {
    "id": "systemctl",
    "label": "systemctl",
    "group": "Système et administration",
    "summary": "Observe l’état des services et unités systemd.",
    "html": "<h2>Comprendre</h2>\n<p>Observe l’état des services et unités systemd.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>systemctl status unite</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>systemctl status sshd</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>status</code> consulte le service tandis que <code>restart</code> le redémarre. Regarde les journaux avant toute action.</p>"
  },
  {
    "id": "tail",
    "label": "tail",
    "group": "Texte et flux",
    "summary": "Affiche les dernières lignes d’un fichier ou suit ses ajouts.",
    "html": "<h2>Comprendre</h2>\n<p><code>tail</code> sélectionne la fin d’un fichier ou d’un flux. La commande affiche les dix dernières lignes par défaut et laisse le fichier source inchangé.</p>\n<p>Elle permet de consulter les informations les plus récentes d’un journal ou de conserver une partie située à la fin d’un résultat.</p>\n<h2>Commandes et options</h2>\n<h3>Afficher les dix dernières lignes</h3>\n<pre><code>tail journaux/rotation</code></pre>\n<p>Sans option, <code>tail</code> affiche les dix dernières lignes dans leur ordre d’origine.</p>\n<h3>Choisir le nombre de lignes</h3>\n<pre><code>tail -n 1 journaux/rotation\ntail -n 4 journaux/rotation</code></pre>\n<p><code>tail -n 1</code> isole la dernière ligne. <code>tail -n 4</code> affiche les quatre dernières lignes.</p>\n<h3>Lire un flux</h3>\n<pre><code>head -n 8 flux/chronologie | tail -n 1</code></pre>\n<p><code>tail</code> reçoit ici les huit lignes sélectionnées par <code>head</code>. Il garde la dernière de ce groupe et isole donc la ligne 8 du fichier d’origine.</p>\n<h3>Suivre les nouvelles lignes</h3>\n<pre><code>tail -f journal.log</code></pre>\n<p>L’option <code>-f</code> laisse la commande active et affiche les nouvelles lignes ajoutées au fichier. Utilise <code>Ctrl-C</code> pour arrêter le suivi et revenir à l’invite.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ tail -n 4 journaux/rotation\nligne 9  contrôle\nligne 10 sauvegarde\nligne 11 FLAG{EXEMPLE}\nligne 12 arrêt</code></pre>\n<p>La commande affiche les quatre dernières lignes. La première ligne visible est donc la neuvième si le fichier en contient douze.</p>\n<h2>Points de vigilance</h2>\n<p><code>tail -n 4</code> affiche un groupe de quatre lignes. Il ne sélectionne pas la quatrième ligne du fichier.</p>\n<p><code>tail -f</code> reste actif jusqu’à son interruption. <code>Ctrl-C</code> arrête la commande sans supprimer le fichier suivi.</p>\n<p>Comme avec <code>head</code>, une redirection vers le fichier source avec <code>&gt;</code> détruirait son contenu avant la lecture.</p>"
  },
  {
    "id": "tar",
    "label": "tar",
    "group": "Système et administration",
    "summary": "Crée, liste ou extrait une archive.",
    "html": "<h2>Comprendre</h2>\n<p>Crée, liste ou extrait une archive.</p>\n<h2>Commandes et options</h2>\n<p>La forme de base est <code>tar -czf archive.tar.gz dossier</code>.</p>\n<h2>Exemple commenté</h2>\n<pre><code>tar -tzf sauvegarde.tar.gz</code></pre>\n<p>La commande ci-dessus illustre une utilisation courante. Adapte les chemins à ton dossier de travail.</p>\n<h2>Points de vigilance</h2>\n<p><code>-c</code> crée une archive, <code>-t</code> la liste et <code>-x</code> l’extrait. Vérifie son contenu avant une restauration.</p>"
  },
  {
    "id": "tr",
    "label": "tr",
    "group": "Texte et flux",
    "summary": "Remplace, supprime ou compresse des caractères dans un flux.",
    "html": "<h2>Comprendre</h2>\n<p><code>tr</code>, abréviation de <strong>translate</strong>, transforme des caractères reçus sur l’entrée standard. La commande travaille caractère par caractère et écrit le résultat sur la sortie standard.</p>\n<p>Contrairement à de nombreuses commandes, <code>tr</code> ne reçoit généralement pas de nom de fichier. Le contenu lui est transmis avec un pipe ou une redirection d’entrée.</p>\n<h2>Commandes et options</h2>\n<h3>Remplacer des caractères</h3>\n<pre><code>cat fichier | tr &#39;a-z&#39; &#39;A-Z&#39;</code></pre>\n<p>Chaque caractère du premier ensemble est remplacé par le caractère correspondant du second ensemble.</p>\n<h3>Normaliser la casse</h3>\n<pre><code>cat normalisation/casse | tr &#39;[:lower:]&#39; &#39;[:upper:]&#39;</code></pre>\n<p>Les classes <code>[:lower:]</code> et <code>[:upper:]</code> représentent les minuscules et les majuscules. Les guillemets empêchent le shell d’interpréter les crochets.</p>\n<h3>Compresser des répétitions</h3>\n<pre><code>cat normalisation/espaces | tr -s &#39; &#39;</code></pre>\n<p>L’option <code>-s</code> réduit chaque suite d’espaces consécutifs à un seul espace. Les colonnes deviennent plus régulières pour un traitement avec <code>cut</code>.</p>\n<h3>Supprimer des caractères</h3>\n<pre><code>tr -d &#39;\\r&#39; &lt; fichier</code></pre>\n<p>L’option <code>-d</code> supprime les caractères indiqués. <code>\\r</code> représente un retour chariot que l’on peut rencontrer dans des fichiers créés sous Windows.</p>\n<h3>Construire un pipeline de normalisation</h3>\n<pre><code>cut -d: -f2 pipeline/incidents | tr &#39;[:lower:]&#39; &#39;[:upper:]&#39; | sort | uniq -c</code></pre>\n<p><code>cut</code> extrait une colonne. <code>tr</code> uniformise sa casse avant le tri et le comptage. Des valeurs qui différaient seulement par les majuscules deviennent alors identiques.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ cat normalisation/casse\nFlag{alpha}\nFLAG{ALPHA}\nflag{beta}\n$ cat normalisation/casse | tr &#39;[:lower:]&#39; &#39;[:upper:]&#39;\nFLAG{ALPHA}\nFLAG{ALPHA}\nFLAG{BETA}</code></pre>\n<p>Après la normalisation, les deux premières lignes sont identiques. Un passage dans <code>sort | uniq -c</code> peut maintenant les compter ensemble.</p>\n<h2>Points de vigilance</h2>\n<p><code>tr</code> transforme des caractères, pas des mots entiers ni des colonnes logiques.</p>\n<p>La commande lit normalement l’entrée standard. <code>tr &#39;a-z&#39; &#39;A-Z&#39; fichier</code> ne fonctionne pas comme <code>cat fichier</code> ou <code>sort fichier</code>.</p>\n<p>Protège les ensembles avec des guillemets. Le shell pourrait autrement interpréter certains crochets ou caractères spéciaux.</p>"
  },
  {
    "id": "uniq",
    "label": "uniq",
    "group": "Texte et flux",
    "summary": "Regroupe, détecte ou compte les lignes identiques qui se suivent.",
    "html": "<h2>Comprendre</h2>\n<p><code>uniq</code> compare chaque ligne à la ligne précédente. Sans option, il remplace chaque groupe de lignes identiques consécutives par une seule ligne.</p>\n<p>La commande ne trie pas les données. Des doublons séparés par d’autres lignes ne sont pas reconnus comme un même groupe. C’est pourquoi <code>sort</code> est souvent utilisé avant <code>uniq</code>.</p>\n<h2>Commandes et options</h2>\n<h3>Retirer les répétitions voisines</h3>\n<pre><code>uniq doublons/brut</code></pre>\n<p>Seules les répétitions consécutives sont regroupées. Les lignes identiques éloignées restent présentes.</p>\n<h3>Afficher uniquement les doublons</h3>\n<pre><code>uniq -d doublons/brut</code></pre>\n<p>L’option <code>-d</code> affiche une fois chaque ligne qui se répète immédiatement.</p>\n<h3>Compter les occurrences</h3>\n<pre><code>uniq -c doublons/brut</code></pre>\n<p>L’option <code>-c</code> place le nombre d’occurrences devant chaque groupe de lignes voisines.</p>\n<h3>Traiter des doublons éloignés</h3>\n<pre><code>sort doublons/brut | uniq\nsort doublons/brut | uniq -d</code></pre>\n<p><code>sort</code> rapproche d’abord toutes les lignes identiques. <code>uniq</code> peut ensuite les regrouper ou afficher uniquement celles qui sont répétées.</p>\n<h3>Classer les fréquences</h3>\n<pre><code>sort doublons/frequences | uniq -c | sort -nr</code></pre>\n<p>Le premier tri regroupe les valeurs. <code>uniq -c</code> les compte. Le dernier tri classe les comptes du plus grand au plus petit.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ cat doublons/brut\nFLAG{ALPHA}\nFLAG{ALPHA}\nFLAG{BETA}\nFLAG{ALPHA}\n$ uniq -d doublons/brut\nFLAG{ALPHA}\n$ sort doublons/brut | uniq -c\n      3 FLAG{ALPHA}\n      1 FLAG{BETA}</code></pre>\n<p>Sans tri, seul le premier groupe d’<code>ALPHA</code> est détecté comme doublon voisin. Après le tri, les trois occurrences se retrouvent ensemble et sont correctement comptées.</p>\n<h2>Points de vigilance</h2>\n<p><code>uniq</code> ne cherche pas dans tout le fichier. Il compare uniquement des lignes voisines.</p>\n<p>Place <code>sort</code> avant <code>uniq</code>, pas après, si tu dois rapprocher des doublons éloignés.</p>\n<p>Les espaces et la casse font partie de la ligne. <code>FLAG{ALPHA}</code>, <code>flag{alpha}</code> et une ligne terminée par un espace sont différents tant qu’ils ne sont pas normalisés.</p>"
  },
  {
    "id": "wc",
    "label": "wc",
    "group": "Texte et flux",
    "summary": "Compte les lignes, les mots et les octets d’un fichier ou d’un flux.",
    "html": "<h2>Comprendre</h2>\n<p><code>wc</code>, abréviation de <strong>word count</strong>, mesure un fichier ou un flux. Sans option, la commande affiche le nombre de lignes, de mots et d’octets.</p>\n<p>Elle ne modifie pas les données. Elle est souvent placée à la fin d’un pipeline pour compter le résultat des étapes précédentes.</p>\n<h2>Commandes et options</h2>\n<h3>Compter les lignes</h3>\n<pre><code>wc -l comptage/candidats/liste</code></pre>\n<p>L’option <code>-l</code> compte les caractères de fin de ligne. Le résultat correspond habituellement au nombre de lignes du fichier.</p>\n<h3>Compter les mots</h3>\n<pre><code>wc -w comptage/phrases/texte</code></pre>\n<p>L’option <code>-w</code> compte les groupes de caractères séparés par des espaces ou d’autres séparateurs reconnus par <code>wc</code>.</p>\n<h3>Compter les octets</h3>\n<pre><code>wc -c comptage/phrases/texte</code></pre>\n<p>L’option <code>-c</code> compte les octets. Avec des caractères accentués, ce nombre peut être supérieur au nombre de caractères visibles.</p>\n<h3>Comparer plusieurs fichiers</h3>\n<pre><code>wc -l comptage/candidats/*</code></pre>\n<p><code>wc</code> affiche une ligne par fichier puis une ligne <code>total</code> lorsque plusieurs fichiers sont mesurés.</p>\n<h3>Compter le résultat d’un pipeline</h3>\n<pre><code>sort comptage/personnes | uniq | wc -l</code></pre>\n<p><code>sort</code> rapproche les noms identiques, <code>uniq</code> n’en garde qu’un par groupe et <code>wc -l</code> compte les noms distincts restants.</p>\n<h2>Exemple commenté</h2>\n<pre><code>$ wc -l comptage/candidats/*\n  8 comptage/candidats/equipe-a\n 12 comptage/candidats/equipe-b\n 20 total\n$ wc -w comptage/phrases/message\n7 comptage/phrases/message</code></pre>\n<p><code>equipe-b</code> contient douze fins de ligne. <code>message</code> contient sept mots selon les règles de découpage de <code>wc</code>.</p>\n<h2>Points de vigilance</h2>\n<p><code>wc -l</code> compte les fins de ligne. Si la dernière ligne ne se termine pas par un retour à la ligne, le résultat peut sembler inférieur au nombre de lignes visibles.</p>\n<p>Lorsqu’un fichier est fourni, son nom apparaît après le nombre. Dans un pipeline, seule la mesure est généralement affichée.</p>\n<p><code>wc -c</code> compte des octets, pas toujours des caractères. Cette différence apparaît notamment avec certains caractères Unicode.</p>"
  }
];
